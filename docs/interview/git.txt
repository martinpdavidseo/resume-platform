Interview questions for git

git add filename.txt
git add .

git commit -m "Your descriptive commit message here"

git push 
git push -u origin develop
------ What it does: It pushes your code and tells Git to remember the relationship between your local develop branch and the develop branch on origin (GitHub).
------ Why use it: You only need to run this the very first time you push a new branch. Once you run it, Git links them up forever.


#new branch
git checkout -b develop


